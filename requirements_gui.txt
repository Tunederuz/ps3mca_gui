# GUI Dependencies for PS2 Memory Card Reader
# Core dependencies (should already be in requirements.txt)
pyDes>=2.0.1
pyusb>=1.2.1

# GUI dependencies
tkinter  # Usually comes with Python, but listed for completeness

# Optional: For better looking GUI (if available)
# pillow>=8.0.0  # For enhanced image support
# ttkthemes>=3.2.0  # For additional themes
